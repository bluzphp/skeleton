<?php $this->script("
    function resourceTabs() {
        $('.tabs').tabs();
    }
")?>
<table class="table table-striped table-bordered table-condensed">
    <thead>
        <tr>
            <th width="180px">User</th>
            <th>Role</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($users as $user):?>
        <tr>
            <td><?=$this->ahref($user->login, ['rcl', 'edit-resources', ['id' => $user->id]], ['shown' => 'resourceTabs', 'class'=>"dialog"]) ?: $user->login?></td>
            <td><?=$user->roles?>
                <?//=$this->ahref($user->getRole(), ['acl', 'edit-user', ['id' => $user->getId()]], ['class' => 'dialog']) ?: $user->getRole()?></td>
        </tr>
        <?php endforeach;?>
    </tbody>
</table>