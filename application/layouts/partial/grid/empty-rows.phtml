<?php
/**
 * @var \Bluz\Grid\Grid $grid
 */
?>
<?php if ($grid->pages() > 1 && sizeof($grid->getData()) < $grid->getLimit()) :
    $emptyRows = $grid->getLimit() - sizeof($grid->getData()); ?>
    <table class="table">
        <?php for ($i = 0; $i < $emptyRows; $i++): ?>
            <tr>
                <td>&nbsp;</td>
            </tr>
        <?php endfor; ?>
    </table>
<?php endif; ?>